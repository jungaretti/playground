{
  "name": "playground",
  "version": "1.0.0",
  "description": "Somewhere to explore new ideas quickly in branches",
  "main": "index.js",
  "scripts": {
    "format": "prettier --write src",
    "format-version": "prettier --version",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "3.0"
  }
}
